{
  "name": "webhook-monorepo",
  "version": "1.0.0",
  "description": "Monorepo for webhook microservices",
  "main": "index.js",
  "scripts": {
    "install:all": "pnpm install",
    "build": "nx build producer && nx build consumer",
    "dev": "pnpm -r --parallel dev",
    "start": "nx run-many --targets=start",
    "test": "nx run-many --targets=test"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.25.0",
  "devDependencies": {
    "@types/node": "^20.10.0",
    "nodemon": "^3.0.0",
    "nx": "22.4.4",
    "ts-node": "^10.9.1",
    "typescript": "^5.3.0"
  }
}