{
  "name": "webhook-monorepo",
  "version": "1.0.0",
  "description": "Monorepo for webhook microservices",
  "main": "index.js",
  "scripts": {
    "install:all": "pnpm install",
    "build": "pnpm -r build",
    "dev": "pnpm -r --parallel dev",
    "start": "pnpm -r start",
    "test": "pnpm -r test"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.25.0",
  "devDependencies": {
    "@types/node": "^20.10.0",
    "nodemon": "^3.0.0",
    "ts-node": "^10.9.1",
    "typescript": "^5.3.0"
  }
}
